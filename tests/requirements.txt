# Zależności do uruchomienia testów integracyjnych LOKALNIE (poza Dockerem).
# psycopg2-binary nie jest tu wymagany — testy używają TASK_BACKEND=memory.
pytest==8.2.0
httpx==0.27.0
pyyaml==6.0.1
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
